<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <title>EVTrip Planner</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        #map { height: 500px; }
    </style>
</head>

<body class="bg-gray-100 text-gray-900">

<header class="bg-white shadow p-6">
    <h1 class="text-3xl font-bold text-blue-600 flex items-center gap-2">
        <i data-lucide="zap"></i>
        EVTrip Planner
    </h1>
</header>

<main class="p-8 grid grid-cols-1 lg:grid-cols-3 gap-8">

<section class="bg-white p-6 rounded-2xl shadow">
    <h2 class="text-xl font-semibold mb-4">Paramètres du trajet</h2>

    <form id="tripForm" class="space-y-4">
        <input id="from" placeholder="Départ (ville ou adresse)"
            class="w-full p-3 rounded border" />

        <input id="to" placeholder="Arrivée (ville ou adresse)"
            class="w-full p-3 rounded border" />

        <select id="vehicle" class="w-full p-3 rounded border"></select>

        <select id="routeMode" class="w-full p-3 rounded border">
            <option value="eco">Économique</option>
            <option value="normal">Normal</option>
            <option value="fast">Rapide</option>
            <option value="safe">Sécurisé</option>
        </select>

        <button type="submit"
            class="w-full bg-blue-600 text-white p-3 rounded-xl font-semibold">
            Calculer le trajet
        </button>
    </form>

    <div id="loader" class="hidden mt-4 text-sm">
        Calcul en cours…
    </div>

    <div class="mt-6 bg-gray-50 p-4 rounded-xl">
        <h3 class="font-semibold mb-2"> Détails du trajet</h3>
        <div class="grid grid-cols-2 gap-3 text-sm">
            <div>Distance : <b><span id="distance">–</span> km</b></div>
            <div>Conduite : <b><span id="drive">–</span> h</b></div>
            <div>Recharge : <b><span id="charge">–</span> h</b></div>
            <div>Arrêts : <b><span id="stops">–</span></b></div>
            <div>Mode : <b><span id="mode">–</span></b></div>
        </div>
    </div>
</section>

<section class="lg:col-span-2 bg-white p-4 rounded-2xl shadow">
    <h2 class="text-xl font-semibold mb-3">Carte du trajet</h2>
    <div id="map" class="rounded-xl"></div>
</section>

</main>

<script src="/static/app.js"></script>
<script>lucide.createIcons();</script>
</body>
</html>
